name: exemplo

on:
  workflow_dispatch:

  push:
    branches:
      - main

jobs:
  hello:
    runs-on: macos-latest
    steps:
      - name: mostrar-mensagem
        run: echo "hello desenv framework II"
